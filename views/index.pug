extends layout

include mixins/_reminderForm
include mixins/_reminders

block content
    .inner
        //- h2 Enter a New Reminder
        //- pre= h.dump(reminder);
        h2 Xmas List 🎅🎄🎁
        +reminderForm()
        p
        +reminders()